{
  "version": 2,
  "builds": [
    {
      "src": "src/index.ts",
      "use": "@vercel/node",
      "config": {
        "distDir": "build"
      }
    }
  ],
  "routes": [
    {
      "methods": ["GET", "POST"],
      "src": "/.*",
      "dest": "src/index.ts"
    }
  ]
}