{
  "version": 2,
  "builds": [
    {
      "src": "src/index.ts",
      "use": "@vercel/node",
      "config": {
        "distDir": "build"
      }
    }
  ],
  "routes": [
    {
      "methods": ["GET", "POST"],
      "src": "/api/store.*",
      "dest": "/api/store"
    }
  ]
}