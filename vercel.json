{
  "builds": [
    {
      "src": "src/*.ts",
      "use": "@vercel/node",
      "config": {
        "distDir": "build"
      }
    }
  ],
  "routes": [
    {
      "methods": ["GET", "POST"],
      "src": "/(.*)",
      "dest": "/index.js"
    }
  ]
}